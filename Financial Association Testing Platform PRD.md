<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# Financial Association Testing Platform PRD

## 项目概述

本项目定义一个面向金融时间序列的通用“关联性检验平台”，目标是把价格、收益率、比值序列、事件标签和多源行情统一纳入一个可审计、可扩展、可重复运行的分析框架，用于回答“两个或多个金融变量之间是否存在统计关联、这种关联是否稳健、是否随时间变化、以及数据本身是否可信”等问题。[^1][^2]

平台首期覆盖 ETF、指数、期货连续合约、宏观时间序列以及用户自有 CSV 数据，支持日频为主、周频与月频兼容的关联性研究流程。与上一版不同，本版本**完整删除 CLI**，所有功能都通过前端 Web 应用完成，包括数据接入、配置管理、分析执行、结果浏览、LLM 解释、审计追踪与报告导出。

该平台不是单一的相关系数脚本，而是一个完整的前端研究工作台，包含数据获取、清洗、口径统一、离散化、连续变量独立性检验、滚动窗口分析、滞后分析、多重检验校正、数据真实性审计，以及一个将“结论与后续建议”交由在线大模型生成的研究解释层。[^2][^1]

## 项目目标

项目的核心业务目标有五个：第一，给量化研究员提供一个标准化的前端关联性检验工作台；第二，降低“随手写脚本”造成的方法漂移和不可复现风险；第三，把统计检验与数据质量审计放在同一交互流程中；第四，用结构化提示词把检验结果转化为专业化研究结论与行动建议；第五，为后续策略研究、因子筛选、风控监测和异常数据预警提供统一底座。[^1]

平台输出不应只告诉用户“显著”或“不显著”，而应同时回答以下问题：关联是分类层面的还是连续变量层面的、是同期关系还是滞后关系、是全样本关系还是阶段性关系、结果是否可能由数据源口径差异驱动，以及在研究、监控或策略场景下应该采取什么下一步动作。[^2][^1]

## 产品定位

平台定位为“前端研究工作台 + 统计引擎服务 + 数据审计引擎 + LLM 研究解释引擎”的四层系统。

其中，前端研究工作台是用户唯一入口，承担参数配置、任务发起、过程可视化、结果解释、导出与留痕；统计引擎负责输出可验证的检验结果，审计引擎负责约束结果可信度，LLM 研究解释引擎只在前两层结果准备完毕且具备结构化上下文后，负责生成结论、风险提示和后续建议。这样可以把计算事实与语言解释分离，减少模型幻觉和不当推断对研究流程的污染。[^1]

## 删除 CLI 的产品决策

本项目不再提供任何命令行入口，不包含 `assoc-test run`、`assoc-test audit`、`assoc-test report`、`assoc-test llm-conclude` 等 CLI 能力。所有原本由命令行承担的配置、运行、对比、审计与导出操作，全部迁移到前端界面中以可视化方式完成。

这样做的原因有三点：其一，统计检验与数据审计包含大量参数和前置条件，前端界面更适合展示口径、假设与风险提示；其二，卡方检验等分类统计方法依赖期望频数、分箱阈值和样本切分，使用可视化表单和即时校验能显著减少误用；其三，最终结论需要结合审计、滚动稳定性和连续变量结果共同解读，前端工作台更适合构建完整的研究路径。[^2][^1]

## 背景与问题定义

卡方独立性检验适用于分类变量频数表，因此金融连续收益率若要进入卡方框架，必须先分箱或离散化，不能直接把原始连续收益率当作卡方输入。[^1][^2]

在长周期金融样本中，极端单日波动可能显著扭曲基于标准差的阈值划分，使大量普通交易日被压缩进“震荡”状态，从而削弱列联表频数均衡性和解释力；因此平台默认需要支持参考期分位数分箱，并在界面中明确显示阈值来源和适用区间。[^1]

统计检验本身只能回答“是否存在统计证据”，却不能自然生成“这意味着什么”和“下一步该怎么做”的专业结论；因此项目需要一个基于结构化检验结果、由在线大模型完成解释与建议的单独层，并且该层必须由前端触发和展示，而不是把未经约束的自然语言总结混入统计流程。[^1]

## 目标用户

目标用户包括量化研究员、金融数据工程师、宏观研究员、资产配置研究员、风险管理团队和需要对多资产联动做统计验证的交易开发者。

典型用户场景包括：检验波动率产品与权益指数的关系、检验黄金与白银或金银比的联动结构、检验宏观变量与风险资产的阶段性关系、比较不同数据源的价格口径一致性，以及把显著性结果输出到研究报告、告警系统或研究助手中。[^2]

## 业务范围

### 首期范围

首期版本必须支持以下输入类型与前端能力：

- 通过前端表单输入公开市场 ticker、时间范围、频率和分析模式。
- 通过前端上传 CSV 文件，接入自有行情、供应商数据和内部清洗后的序列。
- 在前端配置单变量价格序列、多变量价格面板、收益率序列、比值序列与预先离散化后的事件序列。
- 在前端选择时间戳类型、频率和参考期/检验期划分。
- 在前端触发在线大模型分析，生成结论与后续建议。


### 非首期范围

以下能力不纳入 MVP：

- 高频逐笔和盘口微观结构数据的纳秒级检验。
- 实时流式计算和盘中秒级告警。
- 因果识别与结构模型估计。
- 自动交易执行。
- 本地桌面客户端。
- 命令行与终端批处理入口。


## 产品原则

平台设计遵循七项原则：

1. 前端是唯一入口，任何分析功能都必须可在 UI 中完成。
2. 统计检验与数据审计一体化，避免“先信数据、后做统计”的单向流程。[^1]
3. 连续变量方法与分类变量方法并存，避免单一方法带来的解释偏差。[^2][^1]
4. 默认输出效应量、窗口稳定性和校正后显著性，而不是只输出 p 值。
5. 结论生成必须基于结构化事实摘要，而不是原始表格全文喂给模型。
6. 审计失败时，LLM 只能输出保守解释与补充验证建议，不能给出强结论。
7. 所有配置变更都必须在界面中可见、可保存、可回放。

## 核心用例

### 用例 1：前端发起两资产全样本关联性检验

用户在 Web 界面输入两个或多个资产代码与时间范围，系统下载价格数据，构造收益率，执行离散化卡方检验、连续变量独立性检验和相关系数对照分析，并在结果页输出统一摘要。[^2][^1]

### 用例 2：前端配置滚动窗口稳定性分析

用户在参数面板中设定 126、252 或 504 交易日窗口以及步长，平台在每个窗口上重复运行检验，识别关联关系是否只在危机期、牛市期或高波动阶段出现。

### 用例 3：前端执行滞后关系检验

用户在高级设置中设定最大滞后期，平台对每个变量对输出 lag 分析表和最强绝对相关对应的滞后点，用于发现可能的领先—滞后结构。

### 用例 4：前端执行双数据源一致性审计

用户为同一标的在 UI 中同时配置主数据源和第二数据源，平台使用共享阈值构造状态序列，对两个数据源做同质性卡方检验与逐日状态一致率分析，从而识别复权差异、时区错配或缺失修补问题。[^2]

### 用例 5：前端查看数据真实性与异常审计

平台在正式检验前执行缺失值、重复索引、价格冻结、跳点、复权因子变化和期望频数适用性检查，并在结果页以审计卡片、告警条和明细表形式展示，用于判断统计结果是否可能由原始数据质量问题驱动。[^1]

### 用例 6：前端调用 LLM 生成研究结论与后续建议

当统计检验与审计完成后，平台把结构化结果、显著性、稳定性、效应量、数据风险标记和用户问题打包为“研究上下文对象”，由前端触发提交给在线大模型，再以研究结论卡、风险说明块和后续建议清单形式展示在报告页。

## 功能需求

### 模块 A：前端工作台

该模块是本项目的核心。

#### 主要页面

- 首页 / Workspace 选择页
- 新建分析页
- 数据源配置页
- 参数设置页
- 分析执行页
- 结果总览页
- 分类变量检验页
- 连续变量检验页
- 滚动窗口页
- 滞后分析页
- 数据审计页
- LLM 结论页
- 报告导出页
- 历史任务页


#### 核心前端能力

- 可视化表单配置 ticker、日期、频率、派生序列、分箱方法、滚动窗口与滞后参数。
- 文件拖拽上传与字段映射。
- 参数即时校验，例如参考期是否早于检验期、窗口长度是否小于样本长度、卡方适用性风险提示等。[^2][^1]
- 运行中状态展示，包括队列状态、进度条、当前步骤、失败重试。
- 结果的表格、图表、状态标签、审计标记和报告式展示。
- 一键调用 LLM 生成结论与建议。
- 结果保存、复制、下载与分享。


### 模块 B：数据接入层

系统必须支持以下数据接入方式：

- 前端表单方式输入公开市场 ticker，后端通过适配器拉取历史数据。
- 前端上传 CSV，并允许用户在映射弹窗指定 `date_col`、`close_col`、`adj_close_col`、频率和时区。
- 对同一研究任务同时接入主数据源与第二数据源，用于一致性审计。
- 支持原始收盘价与复权收盘价并存，以便执行复权检查。


### 模块 C：数据标准化层

系统必须把输入统一转换为标准研究面板，至少包含：

- `date`
- `instrument`
- `close_raw`
- `close_adj`
- `return_pct`
- `state`
- `source`
- `frequency`
- `timezone`
- `dataset_role`

标准化层负责交易日对齐、缺失处理策略记录、收益率计算和比值序列构造。收益率默认使用百分比表示，便于不同资产和检验口径统一比较。

### 模块 D：离散化层

平台必须支持三类离散化方式：

- 固定阈值分箱。
- 参考期标准差分箱。
- 参考期分位数分箱，默认采用 `33% / 34% / 33%` 三分法。

离散化必须严格区分“参考期定义阈值”和“检验期应用阈值”，并在前端结果页显示阈值来源、取值与样本范围，防止用户误读结果。[^1]

### 模块 E：分类变量检验层

平台必须提供以下卡方类功能：

- 卡方独立性检验：判断两个分类变量是否独立。[^2][^1]
- 卡方拟合优度检验：判断检验期状态分布是否偏离参考期期望概率。[^1]
- 双数据源同质性检验：判断两个数据源的状态分布是否一致。[^2]
- 期望频数适用性检查：输出最小期望频数、低于 5 的单元格占比，并标记是否满足近似条件。[^1]
- 效应量输出：至少输出 Cramer's V，用于衡量分类关联强度。


### 模块 F：连续变量检验层

平台必须提供连续变量的非线性依赖检验，避免把所有问题都强行离散化。首期要求：

- Pearson 相关与 p 值。
- Spearman 相关与 p 值。
- 互信息估计与置换检验，用于捕捉一般统计依赖关系。
- 可插拔扩展接口，便于后续接入 HSIC 等核独立性检验方法。


### 模块 G：滚动窗口分析层

平台必须支持滚动窗口重算以下检验：

- 卡方独立性检验
- 互信息或 HSIC
- Pearson / Spearman
- 数据质量标记

用户可在前端配置窗口长度、步长、最小样本量和显著性水平。输出必须包含每个窗口结束日期、统计量、原始 p 值、校正后 p 值和效应量。

### 模块 H：滞后分析层

平台必须支持负滞后、零滞后和正滞后范围内的关联扫描，并输出：

- 每个 lag 的 Pearson 相关系数
- 每个 lag 的 p 值
- 最大绝对相关对应的 lag
- 对应窗口样本量

结果页必须提供 lag 曲线图和表格双视图，便于研究员同时看全局形态与精确数值。

### 模块 I：多重检验校正层

平台必须支持至少以下多重检验校正：

- Benjamini-Hochberg FDR。
- Benjamini-Yekutieli。
- Bonferroni。

系统应在结果层同时保留 `p_value_raw` 与 `p_value_adjusted`，并在前端明确标识显著性是基于哪一种校正规则。

### 模块 J：数据真实性审计层

该模块是本项目区别于普通统计脚本的关键功能。系统必须支持：

- 缺失值统计、重复索引统计、缺失交易日统计。
- 连续相同价格的 stale run 检测。
- 单日绝对收益率异常阈值检测。
- 基于 MAD 的鲁棒跳点检测。
- 原始收盘价与复权收盘价差异审计。
- 双数据源状态一致率与分布同质性检验。[^2][^1]

前端必须把这些结果转化成用户可理解的风险等级，例如低风险、中风险、高风险，并在 LLM 调用前进行醒目提示。

### 模块 K：LLM 研究解释与建议层

该模块负责把统计层与审计层的结构化输出交给在线大模型生成结论与后续建议，但必须严格限制输入和输出边界。

#### 输入要求

LLM 不直接读取大表，而是读取平台构造的“研究摘要对象”，至少包括：

- 用户问题与研究目的
- 变量定义与金融语义
- 样本区间、频率、参考期/检验期划分
- 分类变量检验结果摘要
- 连续变量检验结果摘要
- 滚动窗口稳定性摘要
- 滞后分析摘要
- 多重检验校正结果
- 数据质量与双源一致性审计标记
- 需要模型回答的任务清单


#### 输出要求

在线大模型必须输出以下结构化字段：

- `executive_conclusion`
- `statistical_interpretation`
- `stability_assessment`
- `data_quality_caveats`
- `market_meaning`
- `next_research_steps`
- `monitoring_suggestions`
- `strategy_risk_notes`
- `confidence_level`
- `forbidden_inference_flags`


#### 安全约束

- 若审计层标记数据高风险，模型必须降低置信度并优先输出补充验证建议。
- 若结果只在少数滚动窗口显著，模型不得将其描述为“稳定规律”。
- 若效应量很弱但 p 值显著，模型必须明确说明“统计显著不等于经济显著”。
- 模型不得生成投资建议式确定性表述。
- 模型不得对未检验的因果关系进行断言。


## 专业提示词体系

### Prompt 设计目标

提示词必须让在线大模型扮演“受约束的金融研究解释器”，而不是自由发挥的市场评论员。提示词应强制模型围绕统计事实、数据质量和下一步研究动作展开，而不是输出泛泛而谈的结论。

### 系统提示词模板

```text
你是一名服务于量化研究团队的金融统计解释助手。
你的任务不是重新做统计检验，而是基于给定的结构化结果，生成专业、克制、可执行的研究结论与后续建议。

硬性要求：
1. 只能依据输入中的结果字段做解释，不得虚构未出现的统计事实。
2. 必须区分统计显著性、效应强度、滚动稳定性和数据质量风险。
3. 如果数据审计存在风险，必须在结论前部突出提示。
4. 不得把相关性表述为因果性。
5. 不得输出确定性的投资建议。
6. 输出应面向专业金融研究人员，使用简洁、准确、克制的术语。
7. 若证据冲突，优先保守结论，并给出进一步验证方案。
```


### 用户任务提示词模板

```text
请基于以下结构化研究结果，输出：
1. 研究结论摘要
2. 分类变量检验与连续变量检验的差异解释
3. 关系是否稳健（结合滚动窗口与滞后分析）
4. 数据质量或数据源一致性是否影响结论
5. 在研究流程中的下一步建议
6. 需要持续监控的指标与触发条件
7. 不能从当前结果推出的内容
```


## 前端信息架构

### 顶部导航

- Workspace
- New Analysis
- Datasets
- Results
- Reports
- History
- Settings


### 新建分析流程

1. 选择数据源类型。
2. 输入 ticker 或上传 CSV。
3. 配置时间范围与频率。
4. 配置参考期 / 检验期。
5. 配置分箱方法、滚动窗口、滞后和校正方式。
6. 预览参数与风险提示。
7. 点击运行。

### 结果页布局

- 左侧：分析配置摘要与风险标记
- 中间：总览卡片、显著关系、审计结论、LLM 入口
- Tab 区：分类检验、连续检验、滚动窗口、滞后分析、数据审计、LLM 结论、原始导出
- 右侧：研究注释、收藏、导出与分享


## UI/UX 要求

- 所有统计方法都必须配有简短说明和“何时使用”提示。
- 卡方检验区域必须显示适用条件提醒，尤其是期望频数限制和分类变量前提。[^1][^2]
- 在参数变更后，界面应提示哪些结果将失效并需要重新运行。
- 高风险审计项必须以颜色、图标和固定提示条显示，不能被折叠隐藏。
- LLM 结果必须与统计原始结果并排可见，避免用户只看自然语言结论。
- 支持桌面端优先设计，同时保证平板可用。


## 输出需求

### 前端可导出文件

项目至少支持导出以下标准文件：


| 文件 | 内容 |
| :-- | :-- |
| `01_prices_raw.csv` | 原始收盘价面板 |
| `02_prices_adj.csv` | 复权收盘价面板 |
| `03_return_panel_pct.csv` | 百分比收益率面板 |
| `04_state_panel.csv` | 离散化状态面板 |
| `05_thresholds.json` | 参考期阈值定义 |
| `06_chi_square_summary.csv` | 分类变量检验结果 |
| `07_continuous_dependency_summary.csv` | 连续变量检验结果 |
| `08_rolling_results.csv` | 滚动窗口检验结果 |
| `09_lag_results_*.csv` | 各变量对的滞后分析 |
| `10_quality_audit.csv` | 缺失、跳点、stale、复权审计 |
| `11_source_consistency.csv` | 双数据源同质性与一致率 |
| `12_llm_context.json` | 提供给在线大模型的结构化上下文 |
| `13_llm_conclusion.md` | 大模型输出的结论与建议 |
| `14_llm_trace.json` | prompt 版本、模型名、时间戳与调用元数据 |
| `15_full_report.html` | 前端导出的完整报告 |

### 报告输出

平台应支持在前端自动生成 Markdown 或 HTML 报告，包含：

- 数据概况
- 方法说明
- 核心显著关系
- 不稳定关系
- 数据质量风险
- LLM 生成的结论与后续建议
- 禁止性推断与研究边界


## 系统架构

### 总体分层

建议采用如下分层：

1. `frontend_app`：唯一用户入口
2. `api_gateway`：任务编排与鉴权
3. `analysis_engine`：统计计算
4. `audit_engine`：数据审计
5. `llm_context_builder`：结构化上下文构造
6. `llm_inference_service`：在线大模型结论生成
7. `report_service`：结果导出与报告生成
8. `storage`：任务、文件与结果存储

### 推荐目录结构

```text
financial-association-platform/
├── apps/
│   └── web/
├── services/
│   ├── api/
│   ├── analysis-engine/
│   ├── audit-engine/
│   ├── llm-service/
│   └── report-service/
├── packages/
│   ├── ui/
│   ├── schemas/
│   └── shared/
├── prompts/
│   ├── system_prompt.txt
│   ├── user_prompt_template.txt
│   └── output_schema.json
├── tests/
├── docs/
└── infra/
```


## 配置设计

平台应通过前端保存分析模板和工作区配置，不再暴露 CLI 配置文件作为主要入口。内部仍可用 JSON/YAML 存储，但用户侧表现为“保存模板”“复制分析”“重新运行同配置”。

### 前端模板字段

- 项目名称
- 数据源配置
- tickers / 上传文件映射
- 时间范围
- 频率
- 派生序列定义
- 参考期 / 检验期
- 分箱方法
- 统计检验选项
- 滚动窗口与步长
- 最大滞后
- 多重检验校正方法
- 审计阈值
- LLM 模型与 prompt 模板版本


## 数据模型

### 主结果表

建议统一使用长表模型，以便未来扩展更多检验方法：


| 字段 | 说明 |
| :-- | :-- |
| `run_id` | 本次运行唯一标识 |
| `test_family` | `categorical` / `continuous` / `audit` / `llm` |
| `test_name` | 具体检验名 |
| `left_series` | 左变量 |
| `right_series` | 右变量 |
| `window_end` | 滚动窗口结束日期 |
| `lag` | 滞后期 |
| `stat_value` | 检验统计量 |
| `p_value_raw` | 原始 p 值 |
| `p_value_adjusted` | 校正后 p 值 |
| `effect_size` | 效应量，例如 Cramer's V |
| `significant` | 是否显著 |
| `notes` | 风险标记或解释 |

### 审计表

审计表必须至少包含：

- `missing_value_count`
- `missing_business_days_count`
- `duplicate_index_count`
- `stale_run_count`
- `jump_count`
- `max_abs_return_pct`
- `adjustment_flag_count`
- `source_match_ratio`
- `audit_status`


### LLM 上下文表

平台必须构建一个独立的 LLM 上下文字段集合，至少包括：

- `research_question`
- `research_scope`
- `sample_info`
- `variable_definitions`
- `categorical_key_findings`
- `continuous_key_findings`
- `rolling_key_findings`
- `lag_key_findings`
- `audit_key_findings`
- `global_confidence_flags`
- `required_answer_sections`
- `forbidden_claims`


## 分析逻辑规范

### 分类变量路线

当用户问题是“涨跌状态是否有关”或“两个数据源的分类分布是否一致”时，优先使用分类变量路线。这一路线需要明确给出离散化方法、参考期阈值、期望频数条件、卡方统计量、p 值和 Cramer's V。[^2][^1]

### 连续变量路线

当用户问题是“收益率大小是否存在一般性依赖”或“是否存在非线性联动”时，优先使用连续变量路线，至少输出 Pearson、Spearman 和互信息置换检验结果。

### 审计优先级

若数据质量审计未通过，例如存在大量缺失交易日、价格冻结、异常跳点或双源状态一致率过低，系统应在结果摘要中将全部统计结论标记为“需谨慎解释”，防止用户把数据错误当成市场规律。[^1]

### LLM 解释优先级

LLM 模块必须遵循以下优先级：

1. 先解释审计风险，再解释统计结果。
2. 先说明稳健性，再说明市场含义。
3. 先给研究建议，再给监控建议。
4. 最后明确当前结果不能支持哪些推断。

## 非功能需求

### 性能

- 5 个变量、5 年日频、全量两两检验应在普通网络环境下于可接受时间内完成。
- 滚动窗口与置换检验应支持后台并行化。
- 在线 LLM 调用失败不应影响统计结果落盘；LLM 结果可重试。
- 前端必须支持长任务的异步轮询或推送更新。


### 可复现性

- 所有参数必须随任务保存并可在前端回放。
- prompt 版本、模型名、调用时间和响应摘要必须记录到 `14_llm_trace.json`。
- 输出文件命名规则必须稳定。
- 数据源版本和下载时间必须记录在任务详情中。


### 可扩展性

- 新的检验方法应通过插件式注册接入。
- 新数据源应实现统一适配器接口。
- 新的 LLM 供应商应实现统一 provider 接口。
- 前端结果层不应绑定某一种图表库。


### 可解释性

- 每个统计量必须有说明字段。
- 报告层必须解释“显著但效应弱”和“窗口显著但全样本不显著”等常见情况。
- 对不满足卡方前提的结果，必须输出明确警告而非静默执行。[^2][^1]
- LLM 输出必须标注置信级别和禁止性推断段落。


## 测试需求

### 单元测试

必须覆盖：

- 收益率计算正确性
- 比值序列计算正确性
- 分位数阈值在参考期固定、在检验期复用
- 卡方统计量与已知样例结果一致
- LLM 上下文构造字段完整性
- 提示词模板渲染正确性
- 前端表单校验与参数映射逻辑


### 集成测试

必须覆盖：

- 从前端建任务到最终报告的整链路运行
- 双数据源一致性比较
- 滚动窗口边界行为
- 无数据、少样本和期望频数不足情形
- 在线大模型调用成功、失败、超时与重试路径
- 结果导出与历史回放


### 审计测试

必须覆盖：

- 缺失交易日注入
- 重复索引注入
- 跳点注入
- 原始价与复权价差异注入
- 第二数据源口径错配注入
- 审计高风险情形下 LLM 是否自动降级为保守输出


## 里程碑规划

### MVP

MVP 应完成：

- 前端建模与运行分析的完整流程。
- ticker 输入与 CSV 上传双输入能力。
- 收益率与比值序列构造。
- 分位数离散化。
- 卡方独立性、拟合优度与同质性检验。[^1][^2]
- Pearson、Spearman、互信息。
- 多重检验校正。
- 缺失、跳点、复权与双源一致性审计。
- 结构化 LLM 上下文构造。
- 在线大模型生成结论与建议。
- HTML / Markdown / CSV 报告导出。
- 历史任务与模板复用。


### V1

V1 应增加：

- HSIC 或其他核独立性检验插件。
- 更丰富的事件标签支持。
- LLM 输出质量评估与模板 A/B 测试。
- 团队协作、批注与分享链接。


### V2

V2 可扩展：

- 多模型共识分析。
- Webhook / API 供外部系统拉取结果。
- 研究项目模板市场。
- 权限、审批流和组织级知识沉淀。


## 风险与限制

卡方检验只适用于分类变量，因此其结论是“状态是否独立”，不能被误读为连续收益率之间的完整依赖刻画。[^2][^1]

金融时间序列常存在自相关、波动聚类和结构变点，因此任何显著性结论都必须结合滚动窗口稳定性和数据质量审计共同解读，不能把单次全样本显著直接等同于稳健规律。

在线大模型擅长把结构化结果转化为研究语言，但不应替代统计引擎和审计引擎本身；如果缺乏结构化输入约束，模型输出很容易滑向泛化评论、因果过度推断和不当投资建议，因此 Prompt 设计、输出 schema 和风险约束属于该项目的核心产品能力，而不是附加功能。

完整删除 CLI 后，平台的可用性会更依赖前端交互设计、长任务调度和结果可视化质量，因此前端不是“展示层”，而是本项目的主产品形态与核心业务入口。

## 成功标准

项目上线后，以以下指标衡量成功：

- 用户能在单次前端操作流内完成从数据接入到审计、检验、LLM 解释和报告导出的全流程。
- 用户能明确区分分类变量结论、连续变量结论、数据质量结论与 LLM 解释层结论。[^1][^2]
- 用户能通过滚动窗口和多重检验校正判断关系是否稳健。
- 用户能通过双源同质性检验与状态一致率快速定位数据口径问题。[^2]
- 用户能用统一提示词体系稳定生成风格一致、边界清晰的研究结论与后续建议。
- 前端模板、历史任务和导出报告能够支持重复研究与团队复用。


## 附：推荐默认方法学

对大多数中频金融研究任务，推荐默认配置如下：

- 参考期 / 检验期拆分，而不是同样本建阈值同样本检验。
- 分位数三分离散化作为默认状态定义。
- 卡方独立性 + Cramer's V 作为分类变量主检验。[^1][^2]
- Pearson + Spearman + 互信息置换检验作为连续变量主检验。
- 252 交易日滚动窗口、21 交易日步长作为默认稳定性分析。
- 最大滞后 10 个交易日作为默认滞后扫描。
- 审计失败优先于显著性解释。[^1]

<div align="center">⁂</div>

[^1]: https://www.jmp.com/zh-hans/statistics-knowledge-portal/chi-square-test

[^2]: https://www.ibm.com/docs/zh/spss-statistics/32.0.0?topic=statistics-tests-independence-chi-square

